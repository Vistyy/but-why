{
  "name": "but-why",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "by": "./bin/by"
  },
  "scripts": {
    "format": "biome format --write .",
    "format-check": "biome format .",
    "lint": "biome lint .",
    "ast-grep-check": "ast-grep test --skip-snapshot-tests && ast-grep scan",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-hero/sandcastle": "0.12.0",
    "@toon-format/toon": "^2.3.0",
    "effect": "3.20.0"
  },
  "devDependencies": {
    "@ast-grep/cli": "^0.44.1",
    "@biomejs/biome": "^2.2.0",
    "@types/node": "^24.0.0",
    "fallow": "^3.2.0",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
