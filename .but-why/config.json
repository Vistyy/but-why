{
  "taskPrefix": "BY",
  "validation": {
    "sandbox": {
      "mode": "none"
    }
  },
  "checks": [
    {
      "id": "quality",
      "command": "pnpm install --frozen-lockfile && just quality",
      "timeoutSeconds": 1200
    }
  ]
}
