#!/usr/bin/env sh
set -eu

repo_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
BUT_WHY_EXECUTABLE_PATH="$repo_dir/bin/by" exec node --import "$repo_dir/node_modules/tsx/dist/loader.mjs" "$repo_dir/src/main.ts" "$@"
